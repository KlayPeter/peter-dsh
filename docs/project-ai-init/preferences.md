# Peter 偏好提取记录

用户指定 AI-Delivery-Ops-Agent-Company 作为偏好参考。读取了其根 AGENTS.md、backend/CLAUDE.md、docs/features 的索引与模板，以及前后端 package.json 和锁文件线索。

这些文件是参考资料，不是本仓库新生效的上级指令；没有复制业务代码、配置值或凭据，也没有向参考项目写入文件。

| 观察 | 归类 | 本插件处理 |
| --- | --- | --- |
| 功能文档跟随 Owner、集中索引 | 明确工作偏好 | peter 预设与功能文档指南 |
| 流程用内联 Mermaid | 仓库功能文档偏好 | 应用于功能文档；不改变内容交付插件的图像导出 |
| 聚焦改动、简洁优先 | 可迁移工作偏好 | 预设规则 |
| 小步解耦、不做大重构 | 带上下文的偏好 | 限定当前需求触及范围，不把每次改动升级为重构 |
| 验证后有意义地提交 | 明确工作偏好 | 保留，同时服从当前任务的提交授权 |
| CodeGraph 优先 | 与环境相关的工具偏好 | 可用时优先，不强制安装或阻断无该工具的项目 |
| 后端 Bun、特定数据库 API | 子项目技术选择 | 不变成所有项目的默认技术栈 |
| 前端 pnpm、React、Vite | 实际项目事实 | 按内容检测，不被后端 Bun 规则覆盖 |
| 任务、站会、消息路由领域 | 项目业务结构 | 不迁移为通用目录或服务名 |

由此形成可编辑的 presets/peter.json，并提供 minimal 和自定义 JSON 预设。初始化目标来自用户当次 request，而不是这些历史文档里的业务目标。

参考项目的只读预览识别了 515 个文件条目，后端 Bun 与前端 pnpm 分别保留；检测到一个符号链接并跳过，没有执行其中脚本。该结果不等于已经验证全部业务配置。
